<p id="notice"><%= notice %></p>
<div class="page-header">
  <h3><%= @project.title %></h3>
  <h3><small>Organization - <%= tenant_name @project.tenant_id %><small></h3>
</div>
<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Details:</strong>
  <%= @project.details %>
</p>

<p>
  <strong>Expected completion date:</strong>
  <%= @project.expected_completion_date %>
</p>

<p>
  <strong>Tenant:</strong>
  <%= @project.tenant %>
</p>

<%= link_to 'Back to Organization Home', root_path %>
<%= link_to 'Edit', edit_tenant_project_path(project: @project, tenant: @project.tenant_id) %> 
<%= link_to 'Destroy', tenant_project_path(project: @project, tenant: @project.tenant_id), :method => 'delete',
            :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?'))},
            :class => 'btn btn-danger'%> 
